<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand nav-link " href="/site/trangchu">Tổng quan</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="/transaction/truyvan" id="navTruyVan">Truy Vấn</a>
        </li>
                <li class="nav-item">
          <a class="nav-link" href="/transaction/date-filter" id="navThucong">Thủ Công</a>
        </li>
        <li class="nav-item">
          <a id="loginLogoutBtn" class="nav-link active" href="/login">Đăng nhập</a>
        </li>
      </ul>
      <!-- Hình ảnh nhỏ góc trên bên phải -->
<a href="https://sepay.vn/" target="_blank">
    <img src="https://sepay.vn/assets/img/logo/sepay-blue-154x50.png" 
         alt="Logo" class="navbar-img">
</a>
    </div>
  </div>
</nav>

<script>
  document.addEventListener("DOMContentLoaded", function () {
      if (window.location.pathname === "/login" || window.location.pathname === "/register") {
          document.querySelector(".navbar-brand").classList.add("disabled-link");
          document.getElementById("navTruyVan").classList.add("disabled-link");
          document.getElementById("navThucong").classList.add("disabled-link");
      }
  });

  document.addEventListener("DOMContentLoaded", function () {
      if (window.location.pathname === "/site/trangchu" || window.location.pathname === "/transaction/truyvan") {
          var loginLogoutBtn = document.getElementById("loginLogoutBtn");
          if (loginLogoutBtn) {
              loginLogoutBtn.textContent = "Đăng xuất";
              loginLogoutBtn.href = "/login"; // Khi bấm vào sẽ đăng xuất
          }
      }
  });
</script>

<style>
  .disabled-link {
      pointer-events: none;
      opacity: 0.5;
      cursor: not-allowed;
  }

  .navbar-img {
      height: 40px; /* Chiều cao ảnh */
      width: auto;
      margin-left: 10px;
      border-radius: 5px; /* Bo góc ảnh */
  }
</style>
