<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng Ký</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            margin: 0; /* Xóa khoảng trắng mặc định */
            padding: 0;
            background-image: url('https://cellphones.com.vn/sforum/wp-content/uploads/2024/04/background-30-4-1-5-2.jpg'); /* hoặc đường dẫn ảnh online */
            background-size: cover;
            background-repeat: no-repeat;
            background-position: top center; /* Căn từ đầu */
            height: 100vh;
            font-family: Arial, sans-serif;
        }
        .register-container {
            max-width: 400px;
            margin: auto;
            margin-top: 50px;
            background-color: rgba(255, 255, 255, 0.2); /* Nền trắng mờ mờ */
            backdrop-filter: blur(5px); /* Hiệu ứng làm mờ nền phía sau */
            -webkit-backdrop-filter: blur(10px); /* Hỗ trợ Safari */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.3);
        }

        .form-control {
            background-color: #4c6d6b;
            border: none;
            color: rgba(0, 0, 0, 0.089);
        }
        .form-control:focus {
            background-color: #6d85dd;
            color: rgb(0, 0, 0);
            box-shadow: none;
            border: 1px solid #3b82f6;
        }
        .btn-primary {
            background-color: #3b82f6;
            border: none;
        }
        .btn-primary:hover {
            background-color: #2563eb;
        }
    </style>
</head>
<body>

<div class="register-container">
    <h2 class="text-center">Đăng Ký</h2>
    <form action="/register" method="POST">
        <div class="mb-3">
            <label class="form-label">Tên đăng nhập</label>
            <input type="text" name="username" class="form-control" placeholder="Tài Khoản" required>
        </div>
        <div class="mb-3">
            <label class="form-label">Mật khẩu</label>
            <input type="password" name="password" class="form-control" placeholder="Nhập mật khẩu" required>
        </div>
        <button type="submit" class="btn btn-primary w-100">Đăng ký</button>
    </form>
    <p class="text-center mt-3">Đã có tài khoản? <a href="/login" class="text-info">Đăng nhập</a></p>
</div>

</body>
</html>
